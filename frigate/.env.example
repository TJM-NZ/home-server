# Copy this file to .env and fill in your values

# Password for the Frigate RTSP restream server (used by clients connecting to 8554)
FRIGATE_RTSP_PASSWORD=changeme

# Password for your Reolink cameras (URL-encoded if it contains special characters)
CAMERA_PASSWORD=your_camera_password_here
