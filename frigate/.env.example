# Copy this file to .env and fill in your values

# Password for the Frigate RTSP restream server (used by clients connecting to 8554)
FRIGATE_RTSP_PASSWORD=changeme

# Password for your Reolink cameras (must be URL-encoded if it contains special
# characters, e.g. ! -> %21, & -> %26). Must be prefixed with FRIGATE_ for
# Frigate's config substitution to work.
FRIGATE_CAMERA_PASSWORD=your_camera_password_here
